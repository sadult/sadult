# راه‌اندازی پروفایل گیت‌هاب @sadult (۵ دقیقه)

## ۱) رپو بساز
یک ریپازیتوری **پابلیک** به نام دقیقاً `sadult` بساز (هم‌نام با یوزرنیمت).
هنگام ساخت، تیک «Add a README file» را بزن.

## ۲) فایل‌ها را آپلود کن
- `README.md` → ریشهٔ ریپو
- `.github/workflows/snake.yml` → انیمیشن Snake
- `.github/workflows/activity.yml` → بخش «Recent activity»

## ۳) اجازهٔ نوشتن به اکشن‌ها بده
Settings → Actions → General → Workflow permissions → **Read and write permissions** → Save.

## ۴) اکشن‌ها را یک‌بار دستی اجرا کن
تب **Actions** → روی هر workflow کلیک کن → **Run workflow**.
بعد از اجرای موفق `Generate Snake Animation`، برنچی به نام `output` ساخته می‌شود و انیمیشن مار در ریدمی ظاهر می‌شود.

## ۵) کارهای باقی‌مانده در README
دو جای فایل کامنت `<!-- TODO -->` دارد:
- توضیح یک‌خطی **Mulberry**
- توضیح یک‌خطی **Dewberry**

اگر نام ریپوها دقیقاً `Mulberry` / `Dewberry` / `HOMA` / `morghie` نیست، در README اصلاحشان کن (کارت‌های pin با نام دقیق ریپو کار می‌کنند و به حروف بزرگ/کوچک حساس‌اند).

## نکته‌ها
- تا وقتی اکشن Snake یک‌بار اجرا نشود، تصویر مار خراب (broken) نشان داده می‌شود. طبیعی است.
- کارت‌های آمار از سرویس‌های عمومی رایگان می‌آیند؛ گاهی چند ثانیه دیر لود می‌شوند.
- پالت رنگ: بنفش `#7C3AED`، آبی `#5E9FE8`، پس‌زمینه `#191919` — هم‌خانواده با برند HOMA.
